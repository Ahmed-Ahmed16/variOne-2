# VariOne S3 — Session Handoff (2026-06-21)

## State
- Branch **finale-fixes**. Local HEAD **b713ae1**; origin/finale-fixes tip **3c2c599**
  → **AHEAD/unpushed by 2** (8c27da2, b713ae1 — both *prior-session* handoff commits,
  not pushed). **No firmware commits this session.**
- Build: **not built this session** — pure LaTeX documentation work, zero firmware edits.
  (`pio run -e varione-s3` NOT run.)
- HW-confirmed: **none this session** (no hardware run).
- Working tree: `Documentation/` is untracked (gitignored doc tree); **all doc edits this
  session live there, NOT in git history**. Pre-existing modified firmware
  (`WifiMenu.cpp`, `evil_portal.cpp`, `wifi_atks.cpp`) still uncommitted — not touched
  this session.

## What shipped (documentation only — no firmware commits; artifact = gitignored doc tree)
All edits to **`Documentation/finale/vari.tex`** (canonical grad paper). Output rebuilt to
**`Documentation/vari.pdf`** (78pg). Verified each round via local XeLaTeX:
**0 errors, 0 missing images, 0 undefined citations, 0 font-shape-undefined warnings.**

| area | change | verified |
|------|--------|----------|
| IEEE refs | `enumerate` → `thebibliography` numeric, 22 `\bibitem{key}` IEEE-format (`"title,"` italic source, `[Online]. Available:` + accessed date), curly quotes; `\renewcommand{\bibname}{References}` | pdftotext shows `[1]…[22]` |
| Citations | 22 in-text `~\cite{}` inserted (Flipper`[1]`…website`[22]`); every ref cited ≥1× | log "NONE undefined" |
| natbib | `\usepackage[numbers,sort&compress]{natbib}` (was bare → would break numeric cite) | cites render `[n]` |
| Acronyms | new **List of Abbreviations** (longtable, ~50 acronyms) after List of Tables, in TOC | rendered, multi-page |
| Cover font | "VariOne" + supervision + date → `\headingfont` (TeX Gyre Bonum), dropped `\underline\scalebox`; was broken `pbk`/`phv` NFSS fallback | p1 clean serif |
| Heading fonts | all `\fontfamily{phv}/{pbk}` → `\headingfont`: chapter covers, TOC/LoF/LoT/Abbrev titles, Appendix cover | 0 font warnings |
| Team table | `tabular*` 0.9\textwidth `@{\extracolsep{\fill}}`, booktabs, **"No." header**, row `\midrule`s, wide spread | p2 |
| Acknowledgments | full-width justified **block** (parindent 0 set AFTER `\justifying`, parskip 1em, no indent) | p3 |
| §1.6 orphan | added intro paragraph under `\section{Project Organization}` | p18 |
| §1.5.8 split | `\needspace{15\baselineskip}` keeps it on one page (was split 3+2) | p19, Resource Opt same page |
| §4.3 ERD float | figure moved to `[H]` right after section note (was buried below A/B/C lists) | p38–39 |
| TOC gap | `\addtocontents{toc}{\protect\vspace{1.2em}}` before `\appendix` | p7 (gap before "A Appendices") |
| Safe fixes | removed `pdfpagemode=FullScreen`, added `citecolor=black`, 6× `Table \ref`→`Table~\ref` | — |
| Packages added | `longtable`, `needspace` | builds |
| Review artifact | full formatting/IEEE review at `~/.claude/plans/read-handoffs-handoff-2026-06-20-md-and-joyful-otter.md` | — |

## Key invariants — DO NOT regress (documentation)
- **`Documentation/finale/vari.tex` is canonical**; build output is **`Documentation/vari.pdf`**
  (NOT `finale/vari.pdf` — that is a STALE 73pg leftover from before this session).
- **Compile = `cd Documentation && xelatex -interaction=nonstopmode finale/vari.tex` ×3.**
  Three passes now mandatory: TOC + `\cite` + longtable + the `\addtocontents` TOC gap all
  need settling.
- **Images live in `finale/Images/` (CAPITAL I).** Lowercase breaks Linux build.
- **References are `thebibliography` numeric** — keep `natbib` **`[numbers]`** option, else
  `\cite` reverts to author-year and errors. 22 `\bibitem` keys; don't revert to `enumerate`.
- **Never reintroduce `\fontfamily{phv}` / `{pbk}`** — those NFSS families don't exist under
  fontspec/XeLaTeX (silent Latin-Modern fallback + warnings). Use `\headingfont` (Bonum).
- **`\usepackage{longtable}` + `{needspace}` required** (acronym list + §1.5.8 keep-together).
- Ack block: `\setlength{\parindent}{0pt}` must be **inside** the group AFTER `\justifying`,
  else indent returns.
- Keep `\subsection{Deliverables...}` backslash + §1.6 intro (orphan-heading guards).

## Open / not done
- **Doc edits not in git history** — `Documentation/` is gitignored; only this handoff is
  committed. The .tex/.pdf are local artifacts only.
- **Overleaf pagination differs from local** (local = 78pg). User compiles on Overleaf;
  re-verify there — fixes are engine-independent but page breaks (needspace/floats) may move.
- **Reference metadata partly inferred** — library refs (`irremote`,`smartrc`,`u8g2`,
  `arduinojson`,`adafruitpn532`,`lovyangfx`,`opensesame`,`espwifitx`) got plausible
  authors/URLs + generic "accessed Jun. 2026"; user should sanity-check accuracy.
- **2 prior-session handoff commits unpushed** (8c27da2, b713ae1) — push when ready.
- 7 overfull hboxes remain (all pre-existing gantt/sequence pages; none from this session).
- IR-TRX BOM line still a 50 EGP ESTIMATE (carried, unconfirmed).

## Next session
Apply any further Overleaf visual tweaks after user reviews the recompiled paper; verify
reference metadata; optionally push finale-fixes. Entry points:
`Documentation/finale/vari.tex`, `Documentation/vari.pdf`,
`~/.claude/plans/read-handoffs-handoff-2026-06-20-md-and-joyful-otter.md` (review).
Relevant memory: [[doc-reconcile-status]], [[varione-site-status]], [[no-push-without-say]].

---

## Session 2 — Graduation documentation COVER print-prep (image work; NO repo/firmware changes)

### State
- Branch **finale-fixes**, HEAD **ed90e8a** → **SYNCED** with origin (that commit is prior/parallel
  firmware work — *not* this session). **No firmware edits, no commits, no build this session.**
- Build: **not built** — `pio run` not run; zero code touched.
- HW-confirmed: **n/a** — print-prep task, no device.
- Working tree: firmware untouched this session. All outputs live **outside the repo**:
  deliverables in `~/Desktop/VariOne_Cover_Print/`, reproducible pipeline in `/tmp/cover_print/`
  (`draw_text.sh`, `build_final.sh`, `measure.sh`). Only this handoff is committed.

### What shipped (deliverables — files, not commits)
| File (~/Desktop/VariOne_Cover_Print/) | What | Status |
|---|---|---|
| VariOne_Cover_FOIL_MASTER_A4bleed.pdf/.png | Primary: white-on-black, A4+3mm bleed, 300dpi | done |
| VariOne_Cover_GOLD_PROOF_A4bleed.pdf/.png | Gold metallic look — approval/visual ONLY | done |
| VariOne_Cover_FOIL_DIE_1bit_bonw/.wonb.pdf/.png | 1-bit foil die art, both polarities | done |
| README_PRINT.txt | Specs + "tell shop: gold foil on black, A4" | done |

Pipeline this session: source = AI render `~/Desktop/ChatGPT Image Jun 21...png` (1023×1538) →
contrast-clean bg to pure black → upscale 2.28× to A4 → kill baked book-frame streaks →
re-typeset team names as real Lato text (AI letterforms had typos) → center the whole vertical
stack (VD/Vemo/VariOne/names) on x=1240 → export.

### Key invariants — DO NOT regress
- **Names truth-source (verbatim, no numbering):** Ahmed Hossam 202201302 / Amir Azzam 202201252 /
  Malak Mohamed 202201430 / Malak Wael 202200599 / Mazen Sadek 202200007 / Mariam Hosam 202200637 /
  Mayan Mohamed 202200346 / Youssef Allam 202201851. Supervisor: Dr. Ahmed Gaber Abuabdallah. 2025-2026.
- **Gold = FOIL stamp** (user-confirmed) → deliver white-on-black master; do NOT tint the master.
- **Raster-edit safety (the load-bearing lesson):** to shift one element in a flat raster, cut ONLY in
  rows verified mean≈0 (per-row MEAN, not maxima — specks fool maxima); for elements overlapping a
  neighbor in Y (logo vs VD), composite via luminance-alpha so black never paints over the neighbor;
  ALWAYS prove untouched regions with `-metric AE -compare == 0` before committing. Verified: logo 0-px.
- Centering applied: VD −92 (alpha-composite), Vemo mascot −42 (feet axis), VariOne wordmark −74,
  names columns centered. VD/Vemo/VariOne now share axis x=1240.

### Open / not done
- **Foil can't do gradients** — flat gold only; the gold render's metallic shading won't reproduce. In README.
- **Shaded mascot doesn't 1-bit cleanly** — die face degrades; recommend foil line-art + print mascot, or
  shop vectorizes. In README.
- User should **eyeball names at full size** once more before sending; **confirm foil vs digital with shop**.

### Next session
- TBD — cover is printer-ready; hand to printer. No firmware impact.
- Entry points: `~/Desktop/VariOne_Cover_Print/` (deliverables), `/tmp/cover_print/` (pipeline scripts).
- Relevant memory: [[grad-cover-print-status]], [[raster-cover-edit-safety]], [[image-print-toolkit]].
