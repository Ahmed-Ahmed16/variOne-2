# VariOne S3 — Session Handoff (2026-06-19)

## State
- Branch **finale-fixes**, tip **a4c89e8**, **SYNCED** with origin
  (`git ls-remote origin finale-fixes` == HEAD). Pushed this session.
- Build: `pio run -e varione-s3` run repeatedly this session, last at a4c89e8 →
  **SUCCESS**. Representative footprint from this session's builds: RAM ~36.5%,
  Flash ~75%.
- Working tree: clean (no uncommitted tracked changes).
- HW-confirmed by user this session (quoted):
  - EMV: "i use a card that usually worked for EMV and now it showed all details again" ✅
  - Debrief: "i joined ap and got debrief on phone" ✅ (report loads end-to-end)
  - Boot splash: "the splash was fine" ✅
  - BACK: "so far the back is working" ✅
  - VariPortal label: "wifi show variportal" ✅

## What shipped (commits oldest->newest)
| commit | what | HW status |
|--------|------|-----------|
| 58fe8d2 | brand: VariOne AP NVS migration, full bruce->vari rename, VariPortal | VariPortal label HW-confirmed |
| e828e5b | menus: restore Jammer Full; hide spectra/IR-jam/clock/wifi-recover | not individually re-tested |
| 351a5b4 | input: global BACK=cancel; debrief on targeted deauth | BACK HW-confirmed working |
| e8b96e3 | IR: instant capture-replay (UP) + clean saved-remote exit | blank-screen fix confirmed; UP-replay untested (button dead on unit) |
| 7b10e76 | rf: full-band NRF HID CH_USB + band-scanner SetRx/threshold | superseded below |
| c4325fd | net: DHCP flush in _setupAP | **REVERTED** (4001a23) — broke all AP joins |
| 5c8ec67 | rfid: EMV RID-fallback classify + full PAN | **HW-confirmed working** |
| 3e00fba | ui: Vemo zoom-in boot splash | **HW-confirmed** |
| 4001a23 | revert DHCP flush; hide NRF mouse/kb jam; band-scanner tweaks | NRF mouse jam hidden (didn't disrupt real dongle) |
| 3d75de6 | rf: band scanner re-strobe RX after retune | did NOT fix it on HW |
| e4b25f0 | debrief: dhcps re-seat w/ set_ip_info | **REVERTED** (fe0f202) — broke association (stations->0) |
| fe0f202 | revert e4b25f0 | — |
| 27b6847 | debrief DHCP-state diagnostic + DEBRIEF_WIFI_FINDINGS.md | diag since removed |
| 299c383 | rf: hide Band Scanner in demo build (dead on HW) | hidden per owner |
| a4c89e8 | debrief: drop the diagnostic (debrief HW-confirmed) | — |

Net effect: branding/menu/IR/EMV/boot-splash/BACK changes stand; the two blind
AP "fixes" were reverted; band scanner + NRF mouse-jam hidden.

## Key invariants — DO NOT regress
- **Debrief/SoftAP is fragile — leave the working formula alone.** Promiscuous-off
  + 800ms BACK-drain + the single shared port-80 server. Real cause of "phone
  associates then drops, no IP" was **polluted NVS** (fixed by full chip erase:
  `pio run -e varione-s3 -t erase`), NOT a code bug — the AP-raise code is
  byte-identical to the confirmed-working 58580a4. See `DEBRIEF_WIFI_FINDINGS.md`.
  - NEVER `softAPConfig()` in the AP path (kills the beacon on this S3).
  - NEVER blind `esp_netif_dhcps_stop/start` in `_setupAP` (broke every AP) or
    `set_ip_info` re-seat (broke association). Both tried and reverted this session.
- **AP creds are now VariOne / varione1** ([config.h:64](src/core/config.h#L64)) with a
  one-time migration in [config.cpp:285](src/core/config.cpp#L285) resetting old Bruce-era SSIDs.
- **EMV classify by 5-byte RID fallback** ([emv_reader.cpp](src/modules/rfid/emv_reader.cpp) `emv_classify`):
  exact-7-byte-AID-only matching was the "all unknown" regression. Keep the RID fallback.
- **Global BACK = cancel / up one level, never select** ([display.cpp loopOptions](src/core/display.cpp));
  VariPortal BACK exits directly, OK opens its options menu ([evil_portal.cpp](src/modules/wifi/evil_portal.cpp)).
- **Hidden demo features stay compiled-out via guards** (code kept in-tree): RSSI/SquareWave/
  Spectrogram, IR Jammer, Clock, WiFi Pass Recovery, Band Scanner, NRF mouse/kb jam —
  via `VARIONE_HIDE_*_EXTRAS` in [varione-s3.ini](boards/varione-s3/varione-s3.ini). Jammer Full stays VISIBLE.
- **Vemo boot splash** behind `VARIONE_VEMO_UI` ([vemo_status.cpp](src/modules/varione/ui/vemo_status.cpp) `vemoBootSplash`).

## Open / not done
- **IR Read replay (UP key)** untested — the UP button is physically dead on this
  unit. Consider remapping replay to DOWN.
- **Band Scanner** + **NRF mouse/kb jam**: hidden, not fixed. Band scanner's CC1101
  RSSI sweep never detected a known 433 TX even after the SetRx retune fix; code is
  in-tree if revisited.
- **Flaky 2nd-debrief "no IP"** (old VARItest issue 7): not reproduced after a clean
  erase; if it recurs in the field, erase NVS first.

## Next session
Deferred work after the FINALE pass (now on origin/finale-fixes @ a4c89e8):
1. **Promote Karma Attack into a guided demo flow** — probe capture -> auto rogue AP
   for a remembered SSID -> auto-pop VariPortal on join -> debrief listing leaked
   SSIDs. Base: [karma_attack.cpp](src/modules/wifi/karma_attack.cpp)
   (`enableAutoKarma`/`enableAutoPortal`/`enableDeauth` already exist) + debrief API
   in [debrief.h](src/modules/varione/debrief/debrief.h). Needs a HW reliability pass
   (probe catch rate, channel, auto-portal handoff).
2. **BadUSB not working** (VARItest issue 20) — USB-mode flags correct in board JSON
   (`ARDUINO_USB_MODE=1` + `CDC_ON_BOOT`); root cause elsewhere, separate investigation.
3. **Local-AI / Gemini debrief wiring** — code present in
   [ai_debrief.cpp](src/modules/varione/debrief/ai_debrief.cpp); cloud Gemini hits
   429 quota; local-LLM fallback via `aiEndpoint` (web form `/aisetup`) needs wiring
   + docs. NOTE: the AI two-phase STA->AP was suspected of destabilizing the debrief
   AP but turned out clean — real bug was NVS pollution.

Build env: `pio run -e varione-s3`. Branch `finale-fixes` is verified demo-clean;
consider folding into `varione-stable` before starting deferred work.
Entry points: src/modules/wifi/karma_attack.cpp, src/modules/varione/debrief/,
boards/varione-s3/varione-s3.ini. Relevant memory: [[finale-tasks-status]],
[[wifi-debrief-working-formula]], [[softap-nvs-rootcause]], [[keyfob-carkey-gap]].
