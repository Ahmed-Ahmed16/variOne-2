# VariOne S3 — Session Handoff (2026-06-18)

## State
- Branch **ai-debrief**, tip `bc8a2f1`. **Unpushed** — `git ls-remote origin ai-debrief`
  returned nothing (no remote branch). Local **ui-revamp** was fast-forwarded to the same
  `bc8a2f1`; origin/ui-revamp is still old `628dc2d`, so ui-revamp is also unpushed/ahead.
- Build: `pio run -e varione-s3` **run this session → SUCCESS** (bin 3,940,208 B ≈ Flash 75%,
  RAM ~36%). `pio run -e esp32-s3-devkitc-1` **run → SUCCESS** (exit 0, portability check).
- HW-confirmed this session (user, on device — running OLD firmware, pre-flash of below commits):
  - BadUSB demo ran: device acted as USB keyboard and typed the payload ("kept typing randomly
    and so fast"; on Linux so no notepad auto-open). 
  - Idle Vemo sleeping screen displays + approved ("VEMO IS SO CUTE") — note this is the **PNG**
    idle art, NOT the new vector face (which is unflashed).
  - **Bug observed:** RGB-noise vertical strip on the **left** edge of the TFT.
- The 4 commits below are **built green but NOT flashed** (USB was busy — SD mounted as Mount
  Storage). None of their behavior is HW-verified yet.

## What shipped (commits oldest->newest)
| Commit | What | HW status |
|--------|------|-----------|
| `e6a75c5` | feat(ui): vector Vemo mascot (drawVemoFace, crisp at any size) + all-stock menu icons (theme keys stripped) + idle 20s→30s | built only, unflashed |
| `323fb0c` | feat(menu): hide BLE Attack Suite / RF "Jammer Full" / RFID 125k+NDEF+SRIX (VARIONE_HIDE_*_EXTRAS); drop hardcoded PIX QR | built only, unflashed |
| `282ae8e` | refactor(storage): SD save paths `/Bruce*`→`/Vari*` (~28 files); **file headers kept "Filetype: Bruce ..."** for Flipper compat; bundles pre-existing SubGhz→LittleFS WIP | built only, unflashed |
| `bc8a2f1` | feat: unified BadUSB awareness debrief (DEBRIEF_BADUSB + arm/runPending hook) + "Grad Projects" beacon spam (17 team names) | built only, unflashed |

> Base commit `de9e325` (AI-powered debrief via Gemini) was also (re)committed/amended this
> session and **absorbed** three of my feedback edits: config.h QR removal (Bruce Wiki/Site),
> board `TFT_OFFSET_Y=0` (RGB-line fix), and the debrief.cpp BadUSB lesson text. It is the AI-debrief
> feature commit; the 4 above sit on top of it.

## Key invariants — DO NOT regress
- **Capture file headers stay "Filetype: Bruce ..."** even though folders are now `/Vari*` —
  Flipper/Bruce parsers depend on header content, not folder name. (`ir_read.cpp:462`,
  `rf_scan.cpp:610`, `PN532.cpp:593`, `srix_tool.cpp`, `chameleon.cpp`.) 16 header lines verified intact.
- **Vemo is now firmware-vector-drawn** (`src/modules/varione/ui/vemo_status.cpp` `drawVemoFace`),
  independent of the SD theme. Old `vemo_*.png` are dead assets; `boot.png` still used at boot.
  Don't reintroduce PNG-based Vemo expressions.
- **Menu hides are reversible compile flags** (`VARIONE_HIDE_*_EXTRAS` in `boards/varione-s3/varione-s3.ini`),
  not deletions — flip a flag to restore (BleMenu/RFMenu/RFIDMenu).
- **AP creds = VariOne / varione1; WebUI = admin / vari** (config.h defaults + live in SD `bruce.conf`).
  Stale persisted config overrides defaults → if BruceNet returns, wipe config/NVS (see [[softap-nvs-rootcause]]).
- **`ai_debrief.cpp` is kept uncommitted / separate** (AI-debrief WIP) per the user's UI-vs-AI split goal —
  do NOT bundle it into UI commits.
- **panel `TFT_OFFSET_Y=0`** is the empirical RGB-line fix (this ST7735S rowstart=0). If a strip
  reappears on another edge, retry OFFSET_Y {0,1,2}/OFFSET_X {1,2,3} per the ini comment.
- ai-debrief == ui-revamp (both `bc8a2f1`). varione-stable untouched. Don't push without asking.

## Open / not done
- **Flash `bc8a2f1` and HW-verify all 4 commits** — none confirmed on device yet (USB busy this session).
- **RGB-line fix unverified** — confirm the left strip is gone after flashing OFFSET_Y=0; iterate if not.
- **BadUSB debrief** — verify the post-run "Debrief?" prompt → SoftAP + join-QR → web report renders.
- **Grad-project beacon** — verify the 17 SSIDs broadcast (WiFi → Wifi Atks → Beacon SPAM → Grad Projects).
- **BLE spam coverage** (Apple/Windows/Android + old phones inconsistent) — deferred upstream payload research.
- **BLE "advertise like before"** — confirm whether user wants iBeacon broadcast vs the existing Popup Spam.
- **Push** ai-debrief/ui-revamp to origin (both unpushed).
- Uncommitted by design: `ai_debrief.cpp`, `ConfigMenu.cpp`, `scratch/ai_debrief_proxy.py`, `.gitignore`,
  marketing PNG deletion — belong to the AI-debrief effort, not this UI work.

## Next session
Flash `bc8a2f1`, run the HW checklist (RGB line, BadUSB debrief, grad beacon spam, vector Vemo
across scan/idle/success/error). Entry points: `src/modules/varione/ui/vemo_status.cpp`,
`src/modules/varione/debrief/`, `src/modules/wifi/wifi_atks.cpp`, `src/modules/badusb_ble/ducky_typer.cpp`,
`boards/varione-s3/varione-s3.ini`. Relevant memory: [[ui-plan-status]], [[ai-debrief-status]],
[[softap-nvs-rootcause]], [[subghz-littlefs]], [[keyfob-carkey-gap]].

---

## Session 2 — AI Debrief: cloud + local-LAN + provider pivot

### State
- Branch **ai-debrief**, tip `bc8a2f1`, **UNPUSHED** (`git ls-remote origin ai-debrief` empty).
- Build: `pio run -e varione-s3` run repeatedly this session; **final SUCCESS** (build10, URL-normalize
  + label fix) — Flash **75.8%**, RAM **36.5%**.
- HW-confirmed this session (user serial / curl):
  - STA connect works (`STA connected: 192.168.0.200`).
  - Cloud Gemini on-device **fails**: TLS `connect: -1`. Root-caused → memory: `free=67372
    largest=31732 psram=0` — two 16KB mbedTLS buffers don't fit one 31KB hole.
  - Key/method validated by **curl 200** with `x-goog-api-key` header; `gemini-3.5-flash`
    intermittently **503 "high demand"**.
  - **Groq** key validated by curl 200 (`llama-3.3-70b-versatile`). Proxy running on PC `192.168.0.119:8000`.
  - Local path reached `localGenerate` but `http.begin()` failed on **`Http://`** (phone capitalized).
- **No AI narrative produced end-to-end on HW yet.** Infra in place; the `Http://` fix is built (build10) but **unflashed**.

### What shipped this session (UNCOMMITTED working tree, on top of `de9e325`/`bc8a2f1`)
| File | Change | Committed? |
|------|--------|-----------|
| `ai_debrief.cpp` | HTTPClient transport; **`x-goog-api-key` header** auth; model `gemini-3.5-flash`; backend dispatch (empty=cloud / set=local plain-HTTP); `parseAiText` (candidates/response/text/raw); mem-instrumented 3× retry; **lowercase scheme normalize** | **uncommitted** |
| `ConfigMenu.cpp` | AI Debrief menu (toggle + AI Setup portal) + **"Test AI (serial)"** | **uncommitted** |
| `scratch/ai_debrief_proxy.py` | LAN proxy: gemini / **groq** / ollama backends | **uncommitted** |
| `boards/varione-s3/varione-s3.ini` | **`-DBOARD_HAS_PSRAM`** (TLS buffers → PSRAM) | committed (in `bc8a2f1` line) |
| `webInterface.cpp/.h` | AI Setup form + endpoint field on shared :80 server | committed |
| `mykeyboard.cpp` | `@`/`!` one-press keys, guarded `VARIONE_VEMO_UI` (btn idx 4/5, FM=1 branch) | committed |

### Key invariants — DO NOT regress
- **AGGREGATE facts only** to any AI (counts/duration/SSID) — never creds/captures/PANs (`ai_debrief.cpp buildPrompt`).
- **Two-phase**: STA fetch → `wifiDisconnect()` → SoftAP serve. Never AP+STA together.
- **Gemini auth = `x-goog-api-key` HEADER**, NOT `?key=`. New keys (`AQ.`-style auth keys) require header; legacy `AIza` retired Sept 2026.
- **PSRAM is mandatory for cloud TLS** — without `-DBOARD_HAS_PSRAM` only ~31KB contiguous heap → mbedTLS `connect -1`. Don't remove.
- `aiEndpoint` empty = cloud Gemini HTTPS; set = local LAN **plain HTTP** (no device TLS). `localGenerate` lowercases the scheme (phones send `Http://`).
- Keyboard `@`/`!` guarded `VARIONE_VEMO_UI`; other boards keep BACK at btn index 4. Don't unbalance `buttons_number`.
- `varione_secrets.h` gitignored — never commit.

### Open / not done
- **Flash build10 + verify the whole AI chain**: local Groq (proxy `POST /` hit + narrative) AND cloud (psram≈2M + HTTP 200). Neither confirmed end-to-end.
- `gemini-3.5-flash` 503-prone → fall back to `gemini-2.0-flash`, or run Groq via proxy as primary.
- **Commit the loose AI files** (`ai_debrief.cpp`, `ConfigMenu.cpp`, proxy, `.gitignore`) — kept uncommitted per the UI-vs-AI split.
- **Rotate exposed keys**: the `gsk_…` Groq key and earlier Gemini key were pasted in chat → compromised.
- Real debrief needs `aiDebriefEnabled` ON (serial smoke test force-enables it).
- Keyboard `@`/`!` unverified on HW across layouts.

### Next session
Flash build10. Proxy on PC (`BACKEND=groq GROQ_API_KEY=… python3 scratch/ai_debrief_proxy.py`); set
`aiEndpoint=http://192.168.0.119:8000/` via AI Setup web form; **Test AI (serial)** → expect proxy hit +
Groq narrative. Then blank endpoint for cloud → confirm `psram≈2,000,000` + 200 (or keep Groq primary).
Commit the loose AI files. Entry points: `src/modules/varione/debrief/ai_debrief.cpp`,
`scratch/ai_debrief_proxy.py`, `src/core/menu_items/ConfigMenu.cpp`, `src/core/mykeyboard.cpp`.
Memory: [[ai-debrief-status]].
