# VariOne S3 — Session Handoff (2026-06-14)

## State
- Branch **`varione-stable`**. HEAD `45dd8dc`. origin tip `9e346fb` → **HEAD is 1 commit ahead (unpushed)**: the `/handoff` command commit. All feature/fix commits through `9e346fb` are on origin (verified `git ls-remote`).
- Build: `pio run -e varione-s3` run repeatedly this session, **SUCCESS** every time; final flash 79.6%, RAM 37%.
- HW-confirmed by user this session: long-press nav; debrief stress (deauth→beacon→deauth all serve); **WebUI restart fixed** (2nd open works); WebUI+debrief mix; keyfob fan = FIXED + scrollable why; **keyfob car key now registers** (final permissive RAW fix).
- Working tree: untracked docs/assets only (FINALEPLAN.md, *.docx, backups/, data/, .claude/settings.json) — none staged.

## What shipped (commits oldest→newest)
| Commit | What | HW |
|---|---|---|
| `a0f486e` | Long-press OK/BACK nav; drivable from 2 buttons (`VARIONE_LONGPRESS_NAV`) | ✓ |
| `e36eb0c` | `showPagedText` scrollable on-TFT viewer (over ScrollableTextArea) | ✓ |
| `d24f46f` | Debrief: fact-driven Session-insight (B3); (early persistent-server attempt) | ✓ |
| `a54f38e`→`1e2450d`→`a7714a1` | Keyfob final: two modes (Any-freq RAW / Keylock) + manual freq pick + **permissive RAW capture** + lenient `rawSimilar` compare | ✓ |
| `88e3060`→`c81b22d` | Port-80 saga final: **one shared persistent AsyncWebServer**, debrief served at `/debrief` via `webDebriefBegin/End` | ✓ |
| `9e346fb` | UImarketing brand assets (user) | — |
| `45dd8dc` | `/handoff` slash command (unpushed) | — |

## Key invariants — DO NOT regress
- **One port-80 owner.** `ensureWebServer()` ([webInterface.cpp](src/core/wifi/webInterface.cpp)) news+begins the shared `AsyncWebServer` once; never `end()`/`free()`. `stopWebUi()` only lowers `isWebUIActive`. Debrief adds no 2nd listener — serves at `/debrief`. AsyncTCP can't rebind port 80 (→ `bind -8` until reboot). See [[wifi-debrief-working-formula]].
- **Debrief URL** = `http://192.168.4.1/` or `/debrief` — no `/debrief/<number>`.
- **Keyfob capture** stays as permissive as RFScan: accept any RAW waveform (`key=0` ok); RAW mode polls only `RAWavailable()`, decode mode only `available()` (merging starves RAW). Radio is OOK/ASK only. See [[keyfob-carkey-gap]].
- Long-press nav is build-flag gated; six short-press directionals still live.
- WiFi/debrief BUG A+B radio-clean formula intact ([[wifi-debrief-working-formula]]).

## Open / not done
- `/handoff` command commit `45dd8dc` **unpushed** — push when ready.
- Keyfob fixed-remote that yields no CRC frame falls to `rawSimilar` string compare (jitter edge case); acceptable, untested at scale.
- Plan B5 (WebUI+debrief path coexistence) effectively done via shared server; no separate-IP work needed.

## Next session
**Focus: NRF24 bring-up** (mousejack / spectrum / jammer); may need HW change; must separate from CC1101.
- Conflict: NRF24 shares HSPI (SCK=12/MOSI=11/MISO=13) with CC1101 (CS=10) **and** the TFT (LovyanGFX, same 11/12). Only CS differs (NRF=15).
- Entry: [nrf_common.cpp](src/modules/NRF24/nrf_common.cpp) `nrf_start()` (SPIClass branch select), features in [src/modules/NRF24/](src/modules/NRF24/), pins in [varione-s3.ini](boards/varione-s3/varione-s3.ini).
- Likely fix: SpiBus mutex around NRF/TFT/CC1101, or dedicated NRF SPI pins (HW). Confirm selected radio path on hardware first.
- Memory: [[nrf24-next-session]], [[varione-stable-branch]].
