# VariOne S3 — Session Handoff (2026-06-20)

## State
- Branch **finale-fixes**, tip **3c2c599**, **SYNCED** with origin
  (`git ls-remote origin finale-fixes` == HEAD). No firmware commits this session.
- Build: **not built this session** — pure documentation work, zero firmware edits.
- HW-confirmed: **none this session** (no hardware run).
- Working tree: `Documentation/` is untracked (gitignored doc tree); all doc edits
  this session live there, NOT in git history.

## What shipped (documentation only — no commits; artifact is the gitignored doc tree)
Canonical grad-paper file finalized. **Filename swapped this session:**
`finale/lasttTTESt.tex` → **`finale/vari.tex`** (canonical, USE THIS);
old Overleaf-matching copy → **`finale/vari_bak_bak.tex`** (backup, no fixes).
`lasttTTESt.*` deleted. Build artifacts: `Documentation/vari.pdf` (good),
`vari_bak_bak.pdf`.

| area | change |
|------|--------|
| Naked headings | Added intro sentences killing section→subsection orphans: Project Organization §1.6, Project Cost Analysis §2.5.1, Selected Driver §A.3; commented redundant `\subsection{Project Schedule}` under Gantt |
| Image paths | All `\includegraphics` rewritten `finale/X` → **`finale/Images/X`** (capital I) to match Overleaf + renamed local dir |
| **Real bug fixed** | §1.6 had `subsection{Deliverables and Key Milestones}` **missing the backslash** → printed as body text + dropped from TOC. Restored to `\subsection{...}`; TOC now shows 1.6.1 Deliverables + 1.6.2 Review (verified via `pdftotext` of TOC page) |
| Ack/Dedication | Acknowledgments got the missing middle paragraph (3 paras now); dedication script font bumped — both render full pages |
| Team table (ported from `vari(check).tex`) | booktabs style (`\toprule/\midrule/\bottomrule`, no vertical bars), `L{}` columntype, headingfont, centered No. column, **"No." header** |
| Title font (ported) | First-page "VariOne" → `\headingfont\bfseries` (TeX Gyre Bonum), dropped underline/scalebox |

## Key invariants — DO NOT regress (documentation)
- **`finale/vari.tex` is THE canonical submission file** (supersedes lasttTTESt naming).
  `finale/vari_bak_bak.tex` = backup only. Don't resurrect lasttTTESt.
- **Images live in `finale/Images/` (CAPITAL I)** — matches Overleaf. All
  `\includegraphics` use `finale/Images/`. Lowercase `finale/images/` will break the
  build (Linux case-sensitive).
- **`\subsection{Deliverables...}` must keep its backslash** — losing it silently
  drops the §1.6 subsection from the TOC and renders it as body text.
- **`\newcolumntype{L}` is required** (added after `\usepackage{array}`) for the Team
  Members booktabs table — don't remove.
- **vari.tex is RICHER than vari(check).tex** — keep its pgfplots effort-curve figure
  (§2.3.1), §3.2.2 Version Control subsection, §4.2.3 Pin Map subsection, and the 10
  granular §4.7.1 subsubsections. vari(check) collapses/drops these; do NOT downgrade.
- Compile: `cd Documentation && xelatex -interaction=nonstopmode finale/vari.tex` ×3.
  Verified this session: **74pg local, 0 missing images, 0 overfull, 0 LaTeX errors.**
  (Overleaf shows 71pg — font/watermark rendering diff; content identical, Overleaf is
  page-count source of truth.)

## Open / not done
- **Optional font-fallback cascade** (from vari(check).tex): `\IfFontExistsTF{Times New
  Roman}{...}{Liberation Serif}{...}` for main/heading/script fonts. This is why
  vari(check) rendered "better on Overleaf" (true Times New Roman). Flagged, NOT
  implemented — preamble swap with some risk; current TeX Gyre fonts render fine.
- **Table-styling screenshot pending** — user said they'll send a "better table"
  styling request next.
- IR-TRX BOM line still a 50 EGP ESTIMATE (carried from prior session, unconfirmed).

## Next session
Apply the user's pending table-styling screenshot to `finale/vari.tex`; optionally
evaluate the Overleaf font-fallback cascade. Entry points:
`Documentation/finale/vari.tex`, `Documentation/vari.pdf`,
`Documentation/finale/vari(check).tex` (reference). Relevant memory:
[[doc-reconcile-status]], [[ai-debrief-status]], [[varione-site-status]].

---

## Session 2 — finale-fixes batch B/C/D (portal verify menu + handshake overflow + badUSB art)

### State
- Branch **finale-fixes**, HEAD **8c27da2** (= Session-1 handoff commit). Remote tip
  **3c2c599** → local is **AHEAD by 1, UNPUSHED** (`git ls-remote origin finale-fixes` = 3c2c599).
- Build: **NOT built this session** — user explicitly held the build ("dont run a build until I come back").
- Firmware changes this session are **uncommitted** (working tree): `evil_portal.cpp`, `wifi_atks.cpp`.
- HW-confirmed this session (user quotes, on the **already-flashed** firmware — NOT these new edits):
  - Portal: *"got the sign in to tnetwork and deauth worked and everythin worked and creds i got it"* —
    captive sheet + deauth + cred capture all work on **Deauth+Clone** path.
  - badUSB: VariOne + Vemo art types into notepad after the charset fix (screenshots confirm).

### What shipped (uncommitted code + untracked payloads)
| Item | What | HW status |
|---|---|---|
| **A** CIC after-login | Verified already-implemented in `loadDefaultHtml_cic()` (htmlPage + afterLoginPage + `cic_creds.csv`). **No change made.** | n/a (pre-existing) |
| **B** verify menu | `evil_portal.cpp:90` — added `{"CIC Login", loadDefaultHtml_cic}` to the **Deauth+Clone+Verify** branch so verify mode serves the same CIC page (was router-only "Default"). | **uncommitted, NOT built/flashed** |
| **C** handshake overflow | `wifi_atks.cpp` `capture_handshake()` — compacted render (dropped blank spacers + indent + Security line, 4 EAPOL lines → 1 `EAPOL: 1Y 2- 3- 4-`); removed now-dead `encryptionTypeStr` switch. | **uncommitted, NOT built/flashed** |
| **D** badUSB art | `sd_files/BadUSB and BlueDucky/` + `badusb_payloads/`: `VariOne_art.txt` (figlet epic), `Vemo_art.txt` (mascot photo-ASCII, `Mo.` ramp), `VariOne_Vemo_art.txt` (merged). Generator: `scratch/gen_badusb_art.py`. | **typed OK on HW** (user-confirmed) |

### Diagnosis corrections (load-bearing — do not re-litigate)
- **"Clone portal blank/no redirect" was NOT a page bug.** `Clone Portal`, `Deauth+Clone`, and standalone
  VariPortal all already call the **same** `loadDefaultHtml_cic()`. Only **Deauth+Clone+Verify** diverged
  (router page) — that's what Part B fixes.
- **Captive sign-in sheet not popping on a targeted clone = OS-side**, not firmware: cloning a *live* SSID →
  victim stays on / OS caches the real internet-having AP → no captive probe. **Deauth+Clone is the lever**
  (forces victim onto the open clone). Standalone works only because `CIC_vari` is a fresh unique name.
  Plain **Clone Portal** on a live AP will rarely pop the sheet.
- **Post-login "no redirect" = working as designed.** `credsController` serves `afterLoginPage` (fake CIC
  home); there is no real redirect because the rogue AP has no uplink. Not a bug.

### Key invariants — DO NOT regress
- **badUSB HID charset:** this device's keyboard layout **silently skips** `# @ * + :` (print() drops them →
  art shows as blank lines). Payload art MUST use the proven palette only: `space ' ( ) , - . / > \ _ | backtick`
  + letters. `scratch/gen_badusb_art.py` **audits** every output against `PROVEN` — keep that gate.
- **Fast reveal:** payloads use `DEFAULTSTRINGDELAY 0` for the near-instant projector "shock". If chars drop
  on a host, bump to `2` (not higher).
- **Part B insertion order:** CIC inserted last in the verify branch ⇒ shown first in the menu (matches non-verify).
- **Part C:** handshake render must stay ≤ ~5 body lines so it never collides with the `tftHeight-35/-20` footer hints.

### Open / not done
- **Part B + Part C: NOT built, NOT flashed, NOT HW-verified.** Build gate (`pio run -e varione-s3`) still owed.
- Firmware edits uncommitted (user controls commits).
- `badusb_payloads/` untracked; payloads also live on the demo SD card (`/media/.../BadUSB and BlueDucky/`).
- Vemo mascot is photo-ASCII (user reverted from a clean hand-drawn 3-pose version back to the photo picture).

### Next session
1. **Build** `pio run -e varione-s3` — primary gate (verify the uncommitted B/C compile; embedded HTML raw-string still balanced).
2. **Flash + HW-verify Part B:** `Deauth+Clone+Verify` → "CIC Login" appears → serves CIC page → submit → `verifyCreds` runs.
3. **HW-verify Part C:** Handshake Capture on a busy AP → no text overflow in `Waiting...` and `CAPTURED!`.
4. Then decide commit/push of the finale-fixes batch (currently 1 unpushed handoff commit + uncommitted B/C).
- Entry points: `src/modules/wifi/evil_portal.cpp`, `src/modules/wifi/wifi_atks.cpp`, `scratch/gen_badusb_art.py`, `sd_files/BadUSB and BlueDucky/`.
- Relevant memory: [[finale-tasks-status]], [[cic-portal-clone-status]], [[badusb-hid-charset]], [[wifi-debrief-working-formula]].
